plugins {
    id 'me.champeau.jmh'
}

jmh {
    humanOutputFile = project.file("${project.buildDir}/results/jmh/human.txt")
    resultsFile = project.file("${project.buildDir}/results/jmh/results.txt")
    zip64 = true
    jmhVersion = "1.35"
}
